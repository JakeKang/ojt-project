// game enviroments
const defaultSize = 10; // 10px
const defaultInterval = 80; // default 80ms
const gameConfig = {
  size: defaultSize,
  width: 40 * defaultSize,
  height: 40 * defaultSize,
};

export { defaultSize, defaultInterval, gameConfig };
